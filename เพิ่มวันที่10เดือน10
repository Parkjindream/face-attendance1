API_URL = f'http://{SERVER_HOST}:{SERVER_PORT}/api/recognize'

# Cache to prevent duplicate submission
last_sent = {}

# Initialize camera
cap = cv2.VideoCapture(0)
if not cap.isOpened():
    print("Cannot open camera")
    exit()
